name: CI
on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup (placeholder)
        run: |
          echo "Set up your language/runtime here"
      - name: Lint (placeholder)
        run: |
          echo "Run lint here"
      - name: Test (placeholder)
        run: |
          echo "Run tests here"
